// material-ui
import { styled } from '@mui/material/styles';

// ==============================|| AUTHENTICATION 1 WRAPPER ||============================== //

const RegWrapper = styled('div')(({ theme }) => ({
    backgroundColor: theme.palette.secondary.main,
    minHeight: '100vh'
  }));

export default RegWrapper;

